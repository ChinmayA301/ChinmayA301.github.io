[project]
name = "aegis-backend"
version = "0.1.0"
description = "Cloudflare Python Worker for Aegis consultative RAG"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "workers-py==1.7.3",
    "workers-runtime-sdk==0.3.1",
]
